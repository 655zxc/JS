2021.11.10

1.
var rg = /abc/
rg.test('abc')
^ $

2.
/^[^a-zA-Z0-9_-]$/

3.
* + ? {num1,num2} {num} {num,} |
/^a{3,16}$/
/^[a-zA-Z0-9_-]{6,16}$/

4.
var reg = /^abc{3}$/; // 它只是让c重复三次   abccc
var reg = /^(abc){3}$/; // 它是让abc重复三次
var reg = /^[abc]{3}$/; //abc任意一个重复三次

5.预定义类

6.参数
g i gi